{
  "name": "trickortreat-local-backend",
  "version": "2.0.0",
  "description": "Local SQLite backend for Trick or Treat Map (No deprecated dependencies)",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "start:prod": "NODE_ENV=production node server.js",
    "dev": "nodemon server.js",
    "init-db": "node init-db.js",
    "test": "node test-api.js",
    "upgrade": "node upgrade.js",
    "setup:env": "cp .env.example .env",
    "clean": "node -e \"const fs=require('fs');try{fs.rmSync('node_modules',{recursive:true});fs.unlinkSync('package-lock.json');}catch(e){}console.log('✅ Cleaned')\"",
    "fresh-install": "npm run clean && npm install"
  },
  "dependencies": {
    "express": "^4.19.2",
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.4.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "keywords": ["halloween", "trick-or-treat", "map", "sqlite", "local", "no-deprecated"],
  "author": "Your Name",
  "license": "MIT"
}